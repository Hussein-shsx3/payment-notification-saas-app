## Add project-specific ProGuard rules here.
## Start with an empty file and add keep rules if your app needs them.

# Example: keep Flutter plugin registrant classes (rarely needed)
#-keep class io.flutter.plugins.** { *; }

# If you see missing-classes warnings during build, add specific -keep rules.
